import sys #this code provides various functions and variables that are used to manipulate different parts of the Python runtime environment.
print("Python version") #this code prints the words "Python version" in the terminal
print(sys.version) #this code prints the python version from the system
print("")
print("Version info") #this code prints the words "Version info" in the terminal
print(sys.version_info) #this code prints the python version info from the system